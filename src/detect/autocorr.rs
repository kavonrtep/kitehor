//! `autocorr` — populated in milestone M1..M5 per `detect_impl_plan.md`.
//!
//! M0 stub. Public surface will appear as algorithms land.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_compiles() {
        // intentionally empty
    }
}
