# Regime C: HOR with div=0.7 — slot consensuses too diverged; detector
# should fall through to simple_TR at HOR-unit width (truth still says
# HOR — see plan §A; classification is the detector's call).
schema_version: 1
seed: 7
global:
  mutation_rate: 0.02
templates:
  t:
    type: HOR_slots
    monomer_length_bp: 170
    k: 4
    inter_slot_divergence: 0.70
structure:
  - type: HOR
    template: t
    n_copies: 200
