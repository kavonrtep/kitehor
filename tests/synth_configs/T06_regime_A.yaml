# Regime A: HOR with div=0 — should classify as simple_TR at base width.
schema_version: 1
seed: 6
global:
  mutation_rate: 0.02
templates:
  t:
    type: HOR_slots
    monomer_length_bp: 170
    k: 4
    inter_slot_divergence: 0.0
structure:
  - type: HOR
    template: t
    n_copies: 200
